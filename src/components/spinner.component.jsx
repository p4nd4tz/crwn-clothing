import { BeatLoader } from "react-spinners";

const Spinner = () => {
  return (
    <div className="flex flex-grow-1 align-middle justify-center">
      <BeatLoader />
    </div>
  );
};

export default Spinner;
