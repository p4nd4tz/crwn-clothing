import { BeatLoader } from "react-spinners";

const Spinner = () => {
  return (
    <div className="flex align-middle justify-center items-center text-center">
      <BeatLoader />
    </div>
  );
};

export default Spinner;